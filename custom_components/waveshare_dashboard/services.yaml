set_page:
  name: Set Page
  description: Set the current page for a Waveshare device
  fields:
    device_id:
      name: Device ID
      description: The ID of the Waveshare device
      required: true
      example: "waveshare_display"
      selector:
        text:
    page_index:
      name: Page Index
      description: The index of the page to display (0-based)
      required: true
      example: 0
      selector:
        number:
          min: 0
          max: 99
          mode: box

next_page:
  name: Next Page
  description: Switch to the next page on a Waveshare device
  fields:
    device_id:
      name: Device ID
      description: The ID of the Waveshare device
      required: true
      example: "waveshare_display"
      selector:
        text:

prev_page:
  name: Previous Page
  description: Switch to the previous page on a Waveshare device
  fields:
    device_id:
      name: Device ID
      description: The ID of the Waveshare device
      required: true
      example: "waveshare_display"
      selector:
        text: